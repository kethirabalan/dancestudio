function addToCart(productName, price) {
    // Logic to add the product to the cart (e.g., store in localStorage, sessionStorage, or send to server)
    alert("You added " + productName + " to your cart!");
  
    // (Optional) Update cart badge or display a confirmation message
  }